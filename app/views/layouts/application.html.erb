<!DOCTYPE html>
<html>
  <head>
    <title>MTTC SMS</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
  </head>

  <body>
    <%= render "layouts/navbar" %>
    <%= render "layouts/flash_messages" %>
    <div class="min-h-screen">
      <%= turbo_frame_tag "cocurriculum_new_edit_modal"%>
      <%= turbo_frame_tag "classroom_new_edit_modal" %>
      <%= turbo_frame_tag "teacher_new_edit_modal" %>
      <%= turbo_frame_tag "term_new_edit_modal" %>
      <%= turbo_frame_tag "student_terminal_subject_new_edit_modal" %>
      <%= turbo_frame_tag "modal" %>
      <%= yield %>
    </div>
    <%= render "layouts/footer" %>
  </body>
</html>
