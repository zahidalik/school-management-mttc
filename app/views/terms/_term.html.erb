<%= turbo_frame_tag(dom_id(term)) do %>
  <div class="bg-sky-500 px-2 py-1 text-lg text-gray-50 rounded-lg">
    <%= link_to term.name, student_term_student_terminal_subjects_url(@student, term), data: {"turbo_frame": "_top"} %>
  </div>
<% end %>