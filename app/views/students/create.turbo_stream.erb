<%= turbo_stream.prepend("students", @student) %>
<%= turbo_stream.update("students_count") do %>
  <span class="px-3 py-3 text-xl text-gray-600 border border-orange-500 rounded-full bg-gray-50"><%= Student.count %></span>
<% end %>
<%# <%= turbo_stream.replace("new_student") do %> %>
  <%# <div class="mt-4 flex flex-col items-center"> %>
    <%# <div class=""> %>
      <%# <%= link_to students_url, class: "px-2 py-1 text-lg text-gray-50 bg-blue-600 shadow-xl rounded" do %> %>
        <%# "Done. Click to refresh the page!" %>
      <%# <% end %>
    <%# </div>
  <%# </div>
<%# <% end %>