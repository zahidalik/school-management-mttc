<% provide(:button_name, "Edit Student") %>
<%# <%= turbo_frame_tag(dom_id(@student)) do %>
  <%# <%= render "form", student: @student %>
<%# <% end %>

<%= turbo_frame_tag "student_new_edit_modal" do %>
  <div data-controller="form-modal" data-action="turbo:submit-end->form-modal#submitEnd">
    <div class="min-h-screen w-screen bg-black bg-opacity-80 flex justify-center items-center fixed z-10">
      <div class="bg-white bg-opacity-100 px-10 py-6 rounded">
        <%= link_to students_path, class: "mx-2 text-xl text-gray-100 absolute top-10 right-10" do %>
          <i class="fa-solid fa-circle-xmark"></i>
        <% end %>
        <div class="my-4 text-2xl text-gray-700 flex justify-center items-center">Edit Student</div>
        <%= render "form", student: @student  %>
      </div>
    </div>
  </div>
<% end %>