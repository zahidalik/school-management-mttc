<%= turbo_stream.append("student_terminal_subjects", @student_terminal_subject) %>

<%= turbo_stream.update("credits_for_#{@student.id}_in_#{@term.id}") do %>
  <div class="px-1 rounded-xl bg-white text-gray-700 border border-cyan-600 text-sm font-bold"><%= @student.credits_in_current_term(@student, @term) %></div>
<% end %>
