<%= turbo_frame_tag(dom_id(subject)) do %>
  <div class="px-3 py-2 bg-teal-200">
    <div class="p-1 bg-teal-400 text-xl text-gray-700">
      <%= subject.name %>
    </div>
    <div class="text-sm text-gray-700">
      <%= subject.book %> |
      <strong><%= subject.credits %></strong>
    </div>
  </div>
<% end %>